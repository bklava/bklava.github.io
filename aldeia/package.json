{
  "name": "aldeia",
  "version": "1.0.0",
  "description": "",
  "main": "aldeia.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "NODE_ENV=development webpack-dev-server --mode development --progress --https --cert /etc/apache2/ssl/server.crt --key /etc/apache2/ssl/server.key",
    "start": "NODE_ENV=development webpack --mode development --watch --progress --https --cert /etc/apache2/ssl/server.crt --key /etc/apache2/ssl/server.key",
    "build": "rm -R dist; NODE_ENV=production webpack --mode production --progress"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "css-loader": "^0.28.11",
    "jquery": "^3.3.1",
    "jsqr": "^1.0.4",
    "node-sass": "^4.9.0",
    "postcss-loader": "^2.1.5",
    "sass-loader": "^7.0.1",
    "style-loader": "^0.21.0",
    "ts-loader": "^4.3.0",
    "typescript": "^2.8.3",
    "webpack": "^4.8.3",
    "webpack-cli": "^2.1.3",
    "webpack-dev-server": "^3.1.4"
  },
  "devDependencies": {
    "@types/jquery": "^3.3.1"
  }
}
